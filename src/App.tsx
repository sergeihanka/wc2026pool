// Router is configured in src/router.tsx and mounted in src/main.tsx via RouterProvider.
// This file is intentionally minimal — kept for potential future layout wrapping.
export { router } from './router'
