// Placeholder test file — ensures Vitest exits 0 during scaffold phase.
// Replace with real tests as features are implemented.
describe('scaffold', () => {
  it('passes', () => {
    expect(true).toBe(true)
  })
})
